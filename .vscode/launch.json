{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "go调试器",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${fileDirname}"
        }
    ]
}